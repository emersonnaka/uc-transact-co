# Qwen — a question becomes a structural blocker

**Model:** `qwen/qwen3.8-max` via OpenRouter · **Confidence:** 72/100

> Move “done” from scheduler prose into the task and the surrounding components
> become replaceable.

**What it saw.** A ready physical staging task and a Finance-owned Revenue
question should not look similar in the backlog: the former is executable; the
latter maps to a blocked or input-required state.

**Best contribution.** It connected ontology ownership, structural blocking,
and agent protocol lifecycle especially well.

**Important restraint.** The database example is illustrative. For the actual
Day 3 build, use `stg_customers`; `stg_orders` already exists from Day 2.

**Use in the room.** This is the strongest bridge from TransactCo to Task-Spec.
