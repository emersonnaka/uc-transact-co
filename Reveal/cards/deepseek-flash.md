# DeepSeek Flash — the failed run

**Model:** `deepseek-v4-flash` · **Result:** `SOURCE_ACCESS=FAILED`

> No source, no analysis.

**What happened.** The model followed the fail-closed instruction and returned
only the required failure marker.

**Why this belongs in the Reveal.** Agent quality is irrelevant if the runtime
cannot reach the approved evidence. Context availability is a prerequisite,
not an inconvenience to hide with prior knowledge.

**Use in the room.** Reveal this card before the eight successful cards. It
connects Day 1 context discipline to Day 3 task execution in one moment.
